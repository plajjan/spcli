<cli>
<operational>
	# show ntp status
	# show ntp associations
	# show ntp peer X
	# request process restart ntp
	<branch name="show">
		<branch name="ntp">
			<leaf name="status" command="ntpq" args="-q">
			<leaf name="pen" command="pen_wrapper" args="show bajs">
			</leaf>
			<branch name="peer">
				<????>
			</branch>
		</branch>
	</branch>
</operational>
<configuration>
	# embedded DTD
</configuration>
</cli>
<configuration>
<template>
# Generated by {{app_name}}
{% if var_system_search_domain -%}
search {{var_system_domain}}
{%- for domain_search in var_system_search_domain %} {{domain_search}}{% endfor %}
{% else -%}
domain {{var_system_domain}}
{% endif -%}
{% for nameserver in var_nameservers %}nameserver {{nameserver}}
{% endfor %}

{% for nameserver in config.xml_xpath("//name-server") %}nameserver {{nameserver}}
{% endfor %}

{% for ntp_server in config.xml_xpath("//ntp/server") %}ntp {{ntp_server.name}}
{% endfor %}
</template>
<location>/etc/resolv.conf</location>
</configuration>
